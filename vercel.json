{
  "version": 2,
  "builds": [
    {
      "src": "off-topics-main/package.json",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/off-topics-main/$1"  // Adding a slash before the folder name
    }
  ]
}
